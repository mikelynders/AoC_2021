from dataclasses import dataclass
from typing import List
from utils import transpose

@dataclass
class Board():
    columns: List[List[int]]

    def transpose(self):
        return 

    def testColumn():
        

    def mark(int):
        for row in 

    def isWin():
        self.checkRows() or checkRows(transpose(self.data))

    def score():

def checkBoard(board: Board):
    return board.checkRow() or checkRow(tranposeBoard(board))

def checkHorizontal(board: Board):
    return True

def checkVertical(board: Board):
    return True







def scoreBoard(board: Board) -> int:
    return 0

 balls = []
 boards = []

for ball in balls:
    for board in boards:
        board.isWinner()


def recursiveBingo(numbers, boards):

    boards = checkBoards

    if winner():
        return board.score()

    return recursiveBingo(numbers[1:], boards)
